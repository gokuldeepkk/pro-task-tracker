export const API_VERSION = "/v1";

export const BASE_ROUTES = {
  public: "/public",
  secure: "/secure",
};

export const MODULES = {
  users: "/users",
  tasks: "/tasks",
  projects: "/projects",
};

export const PUBLIC_ROUTES = {
  health: "/health",
  signin: "/signin",
  register: "/register",
};
